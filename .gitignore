node_modules/
.pnpm-store/
dist/
.next/
out/
build/
*.tsbuildinfo

.env
.env.*
!.env.example

# plugin build output
plugin/**/bin/
plugin/**/obj/
# Release zips are build artifacts, ~6MB each, and are uploaded to GitHub
# releases rather than committed. plugin/manifest.json IS committed -- Jellyfin
# reads it straight from the repo.

coverage/
.DS_Store
*.log
