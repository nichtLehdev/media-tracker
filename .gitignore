node_modules/
.pnpm-store/
dist/
.next/
out/
build/
*.tsbuildinfo

.env
.env.*
!.env.example

# plugin build output
plugin/**/bin/
plugin/**/obj/
# Release zips are build artifacts, ~6MB each. The manifest beside them IS
# committed; run plugin/build-plugin.sh --publish before deploying so the
# sourceUrl it advertises actually resolves.
apps/web/public/plugin/*.zip

coverage/
.DS_Store
*.log
